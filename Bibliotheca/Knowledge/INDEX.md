# Index

> The one index of everything in `Bibliotheca/Knowledge/`, grouped by domain, one line per
> article: a standard markdown link and the sentence that says what the article settles. Rebuilt by
> the researcher's `/compile` and `/refresh-index`; never edited by hand outside those runs.
> **Read this first, always** — a question is answered by walking the index and the links before
> opening any article.

*(empty — nothing compiled yet. Drop a source into `Bibliotheca/Papers/`, `Books/` or `Notes/`
and have the researcher run `/compile`.)*
